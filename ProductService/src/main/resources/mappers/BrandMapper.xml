<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gsvn.productservice.mapper.BrandMapper">

    <resultMap id="BrandResultMap" type="com.gsvn.productservice.model.entity.Brand">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="description" column="description" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="BrandResultMap">
        SELECT id, name, description, created_at,updated_at
        FROM brand
        WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="BrandResultMap">
        SELECT id, name, description, created_at,updated_at
        FROM brand
        ORDER BY id ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO brand (name, description)
        VALUES (#{name}, #{description})
    </insert>

    <update id="update">
        UPDATE brand
        SET name = #{name},
            description = #{description}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM brand WHERE id = #{id}
    </delete>
    <select id="findPage" resultMap="BrandResultMap">
        SELECT * FROM brand
        <where>
            <if test="keyword != null and keyword != ''">
                AND (LOWER(name) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
                OR LOWER(description) LIKE LOWER(CONCAT('%', #{keyword}, '%')))
            </if>
        </where>
        ORDER BY ${sortField} ${sortOrder}
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countSearch" resultType="long">
        SELECT COUNT(*) FROM brand
        <where>
            <if test="keyword != null and keyword != ''">
                AND (LOWER(name) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
                OR LOWER(description) LIKE LOWER(CONCAT('%', #{keyword}, '%')))
            </if>
        </where>
    </select>

    <select id="existsById" resultType="boolean">
        SELECT EXISTS (
            SELECT 1 FROM brand WHERE id = #{id}
        )
    </select>

    <select id="existsByName" resultType="boolean">
        SELECT EXISTS (
        SELECT 1 FROM brand
        WHERE LOWER(name) = LOWER(#{name})
        <if test="excludeId != null">
            AND id != #{excludeId}
        </if>
        )
    </select>
</mapper>