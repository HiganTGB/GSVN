<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gsvn.inventoryservice.mapper.WarehouseMapper">

    <resultMap id="WarehouseMap" type="com.gsvn.inventoryservice.model.entity.Warehouse">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="code" column="code" />
        <result property="branchId" column="branch_id" />
        <result property="staffId" column="staff_id"/>
        <result property="isActive" column="is_active" />
        <result property="contactName" column="contact_name" />
        <result property="contactPhone" column="contact_phone" />
        <result property="addressDetail" column="address_detail" />
        <result property="provinceCode" column="province_code" />
        <result property="wardCode" column="ward_code" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO warehouses (
            name, code, branch_id, staff_id, is_active, contact_name, contact_phone,
            address_detail, province_code, ward_code
        ) VALUES (
                     #{name}, #{code}, #{branchId}, #{staffId}, #{isActive}, #{contactName}, #{contactPhone},
                     #{addressDetail}, #{provinceCode}, #{wardCode}
                 )
    </insert>

    <update id="update">
        UPDATE warehouses
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="branchId != null">branch_id = #{branchId},</if>
            <if test="staffId != null">staff_id = #{staffId},</if>
            <if test="isActive != null">is_active = #{isActive},</if>
            <if test="contactName != null">contact_name = #{contactName},</if>
            <if test="contactPhone != null">contact_phone = #{contactPhone},</if>
            <if test="addressDetail != null">address_detail = #{addressDetail},</if>
            <if test="provinceCode != null">province_code = #{provinceCode},</if>
            <if test="wardCode != null">ward_code = #{wardCode},</if>
            updated_at = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <select id="findById" resultMap="WarehouseMap">
        SELECT * FROM warehouses WHERE id = #{id}
    </select>

    <select id="findByCode" resultMap="WarehouseMap">
        SELECT * FROM warehouses WHERE code = #{code}
    </select>

    <select id="findAllPaged" resultMap="WarehouseMap">
        SELECT * FROM warehouses
        <where>
            <if test="keyword != null and keyword != ''">
                AND (name ILIKE CONCAT('%', #{keyword}, '%') OR code ILIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY ${sortField} ${sortOrder}
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM warehouses
        <where>
            <if test="keyword != null and keyword != ''">
                AND (name ILIKE CONCAT('%', #{keyword}, '%') OR code ILIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>

    <select id="findAll" resultMap="WarehouseMap">
        SELECT * FROM warehouses
        ORDER BY id DESC
    </select>

</mapper>