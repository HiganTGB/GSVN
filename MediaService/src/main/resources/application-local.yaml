spring:
  security:
    user:
      name: ${MEDIA_SERVICE_USER:abc1234}
      password: ${MEDIA_SERVICE_PASSWORD:abc1234}
  cloud:
    consul:
      host: ${CONSUL_HOST:localhost}
      port: ${CONSUL_PORT:8500}
      discovery:
        service-name: media-service
        instance-id: ${spring.application.name}:${server.port}
        prefer-ip-address: false
        heartbeat:
          enabled: true
          ttl: 300s
        health-check-critical-timeout: 12m
jwt:
  signerKey: ${JWT_SIGNER_KEY:5661608670d8591f4a9b407b975e1195d5204481358997c4e512803b87944062}
minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:3WJhMQqaAIvGH5aIXNyW}
  secret-key: ${MINIO_SECRET_KEY:2khm48vrXXpJhSkapWcQhvUD4c4A3oE0wtIZXTBE}
  external-url: ${MINIO_EXTERNAL_URL:http://localhost:9000}
