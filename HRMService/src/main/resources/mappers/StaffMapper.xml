<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gsvn.hrmservice.mapper.StaffMapper">
    <select id="findListByActive" resultType="com.gsvn.hrmservice.model.entity.Staff">
        SELECT *
        FROM STAFFS
        WHERE is_active = #{isActive}
          AND deleted_at IS null
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="staffId" keyColumn="staff_id">
        INSERT INTO STAFFS (user_id, full_name, email, dob, gender, phone_number,address,
                            identity_card, warehouse_id, position_id, base_salary)
        VALUES (#{userId}, #{fullName}, #{email}, #{dob}, #{gender}, #{phoneNumber},#{address},
                #{identityCard}, #{warehouseId}, #{positionId}, #{baseSalary})
    </insert>

    <update id="update">
        UPDATE STAFFS
        <set>
            <if test="fullName != null">full_name = #{fullName},</if>
            <if test="email != null">email = #{email},</if>
            <if test="dob != null">dob = #{dob},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phoneNumber != null">phone_number = #{phoneNumber},</if>
            <if test="address != null">address = #{address},</if>
            <if test="identityCard != null">identity_card = #{identityCard},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="warehouseId != null">warehouse_id = #{warehouseId},</if>
            <if test="positionId != null">position_id = #{positionId},</if>
            <if test="baseSalary != null">base_salary = #{baseSalary},</if>
            updated_at = CURRENT_TIMESTAMP
        </set>
        WHERE staff_id = #{staffId} AND deleted_at IS null
    </update>

    <select id="findAdvanced" resultType="com.gsvn.hrmservice.model.entity.Staff">
        SELECT *
        FROM STAFFS
        <where>
            <if test="keyword != null and keyword != ''">
                AND (full_name ILIKE CONCAT('%', #{keyword}, '%')
                OR email ILIKE CONCAT('%', #{keyword}, '%')
                OR phone_number LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="warehouseId != null">AND warehouse_id = #{warehouseId}</if>
            <if test="positionId != null">AND position_id = #{positionId}</if>
            AND deleted_at IS  null
        </where>
        ORDER BY ${sortField} ${sortOrder}
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAdvanced" resultType="int">
        SELECT COUNT(*) FROM STAFFS
        <where>
            <if test="keyword != null and keyword != ''">
                AND (full_name ILIKE CONCAT('%', #{keyword}, '%')
                OR email ILIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="warehouseId != null">AND warehouse_id = #{warehouseId}</if>
            <if test="positionId != null">AND position_id = #{positionId}</if>
            AND deleted_at IS null
        </where>
    </select>

    <select id="findById" resultType="com.gsvn.hrmservice.model.entity.Staff">
        SELECT * FROM STAFFS WHERE staff_id = #{id} AND deleted_at IS  null
    </select>

    <select id="findByUserId" resultType="com.gsvn.hrmservice.model.entity.Staff">
        SELECT * FROM STAFFS WHERE user_id = #{userId} AND deleted_at IS  null
    </select>

    <update id="deleteById">
        UPDATE STAFFS SET deleted_at = CURRENT_TIMESTAMP, updated_at = CURRENT_TIMESTAMP WHERE staff_id = #{id}
    </update>

    <update id="updateActive">
        UPDATE STAFFS SET is_active = #{isActive}, updated_at = CURRENT_TIMESTAMP WHERE staff_id = #{staffId}
    </update>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM STAFFS
        WHERE email = #{email}
        AND deleted_at IS  null
        <if test="staffId != null">
            AND staff_id != #{staffId}
        </if>
    </select>

    <select id="existsByIdentityCard" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM STAFFS
        WHERE identity_card = #{identityCard}
        AND deleted_at IS  null
        <if test="staffId != null">
            AND staff_id != #{staffId}
        </if>
    </select>
</mapper>