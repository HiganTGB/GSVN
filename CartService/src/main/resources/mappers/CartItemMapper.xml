<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gsvn.cartservice.mapper.CartItemMapper">

    <resultMap id="CartItemResultMap" type="com.gsvn.cartservice.model.entity.CartItem">
        <id property="id" column="id"/>
        <result property="cartId" column="cart_id"/>
        <result property="skuId" column="sku_id"/>
        <result property="isDeposit" column="is_deposit"/>
        <result property="quantity" column="quantity"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart_item (cart_id, sku_id, is_deposit, quantity)
        VALUES (#{cartId}, #{skuId}, #{isDeposit}, #{quantity})
    </insert>

    <select id="findById" resultType="com.gsvn.cartservice.model.entity.CartItem">
        SELECT id, cart_id, sku_id, is_deposit, quantity, created_at, updated_at
        FROM cart_item
        WHERE id = #{id}
    </select>

    <select id="findByCartIdAndSkuId" resultType="com.gsvn.cartservice.model.entity.CartItem">
        SELECT id, cart_id, sku_id, is_deposit, quantity, created_at, updated_at
        FROM cart_item
        WHERE cart_id = #{cartId} AND sku_id = #{skuId}
    </select>
    <select id="findByCartId" resultType="com.gsvn.cartservice.model.entity.CartItem">
        SELECT id, cart_id, sku_id, is_deposit, quantity, created_at, updated_at
        FROM cart_item
        WHERE cart_id = #{cartId}
    </select>

    <update id="updateQuantity">
        UPDATE cart_item
        SET quantity = #{quantity}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateQuantityAndDeposit">
        UPDATE cart_item
        <set>
            <if test="quantity != null">
                quantity = #{quantity},
            </if>
            <if test="isDeposit != null">
                is_deposit = #{isDeposit},
            </if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM cart_item WHERE id = #{id}
    </delete>

    <delete id="deleteByCartId">
        DELETE FROM cart_item WHERE cart_id = #{cartId}
    </delete>

</mapper>