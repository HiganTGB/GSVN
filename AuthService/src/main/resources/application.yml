spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:prod}
  application:
    name: auth-service

server:
  port: ${AUTH_SERVICE_PORT:5001}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always